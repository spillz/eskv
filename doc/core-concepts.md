# Core Concepts

Summarize the widget tree, event propagation, resource management, and animation model. Explain how `EventSink`, `WidgetAnimation`, and `Resource` collaborate to keep the interface responsive.
