# Project Setup

Document the steps for installing dependencies, running the Vite development server, and embedding the `lib/eskv.js` module in a standalone HTML page. Include tips for clearing caches when editing ES modules served statically.
